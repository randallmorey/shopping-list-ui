<button
  type="button"
  class="button-delete{{if @loading " is-loading"}}"
  disabled={{if @disabled "disabled"}}
  ...attributes>
  {{#if hasBlock}}
    {{yield}}
  {{else}}
    {{t "actions.default.delete"}}
  {{/if}}
</button>
