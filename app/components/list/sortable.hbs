<List::List
  class="list-sortable"
  @ordered={{true}}
  {{sortable-group onChange=@onChange itemVisualClass="active"}}>

  {{yield (hash item=(component "list/item/sortable" disabled=@disabled))}}

</List::List>
