<Layouts::LayoutSplit as |layout|>

  <layout.pane @active={{this.activePane.isSidebar}}>
    {{outlet "sidebar"}}
  </layout.pane>

  <layout.pane @active={{this.activePane.isBody}}>
    {{outlet}}
    items
  </layout.pane>

</Layouts::LayoutSplit>
